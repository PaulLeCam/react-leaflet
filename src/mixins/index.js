import element from "./element";
import mapLayer from "./mapLayer";
import popupContainer from "./popupContainer";
import tileLayer from "./tileLayer";

export default {element, mapLayer, popupContainer, tileLayer};
